;HARSHIT SRIVASTAVA 19BCE0382
DATA SEGMENT
NUM DW 05,04
LCM DW 2 DUP(0) 
GCD DW 2 DUP<0>
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START: MOV AX,DATA
MOV DS,AX
MOV DX,0H
MOV AX,NUM
MOV BX,NUM+2
UP: PUSH AX
PUSH DX
DIV BX
CMP DX,0
JE EXIT
POP DX
POP AX
ADD AX,NUM
JNC DOWN
INC DX
DOWN: JMP UP
EXIT: POP LCM+2
POP LCM  
XOR AX,AX
XOR BX,BX
MOV AX,NUM
MOV BX,NUM+2
MUL BX
DIV LCM
MOV GCD,AX
MOV AH,4CH
INT 21H
CODE ENDS
END START